# pyrefly.toml - Pyrefly type checker configuration
# Suppresses phantom parse errors from virtual in-memory f-string analysis

[project]
# Exclude Pyrefly's own virtual in-memory scratch files from error reporting
exclude = [
    "__pyrefly_virtual__/**",
    "**/inmemory/**",
]
extraPaths = ["backend"]
